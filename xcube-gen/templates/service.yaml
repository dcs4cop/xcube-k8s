apiVersion: v1
kind: Service
metadata:
  name: {{ include "xcube-gen.fullname" . }}
  labels:
    {{- include "xcube-gen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "xcube-gen.selectorLabels" . | nindent 4 }}
