apiVersion: v1
kind: Service
metadata:
  name: "xcube-gen"
  namespace: "xcube-gen"
spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: NodePort
  selector:
    app: "xcube-gen"
